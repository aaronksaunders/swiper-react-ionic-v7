import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'swiper-ionic-v7',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
